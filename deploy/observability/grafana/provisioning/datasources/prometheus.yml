# Grafana datasource provisioning — 启动时自动建 Prometheus 数据源
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
    uid: prometheus
    editable: true
    jsonData:
      timeInterval: 15s
      httpMethod: POST
